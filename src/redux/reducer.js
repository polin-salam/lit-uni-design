
// import { createSelector } from 'reselect'; // it will be use when we dont want with each render data be filtered or any thing with this 

export const reducer = (state = '', action) => {
    switch (action.type) {
      default:
        return state;
    }
  };